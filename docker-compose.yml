services:
  web:
    build: .
    container_name: fidden_web
    working_dir: /app
    command: ["gunicorn", "fidden.wsgi:application", "--chdir", "/app", "--bind", "0.0.0.0:8090"]
    volumes:
      - media:/app/media
      - staticfiles:/app/staticfiles
    ports:
      - "8090:8090"
    depends_on:
      - redis
    env_file:
      - .env

  celery:
    build: .
    container_name: fidden_celery
    working_dir: /app
    command: celery -A fidden worker --loglevel=info
    volumes:
      - ./:/app
    depends_on:
      - redis
    env_file:
      - .env

  celery-beat:
    build: .
    container_name: fidden_celery_beat
    working_dir: /app
    command: celery -A fidden beat --loglevel=info
    volumes:
      - ./:/app
    depends_on:
      - redis
    env_file:
      - .env

  redis:
    image: redis:8
    container_name: fidden_redis
    ports:
      - "6385:6385"

volumes:
  media:
  staticfiles:
